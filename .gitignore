# build outputs
/build/
/out/
/dist/
/tmp/

# fetched upstream trees (we don’t vendor these; we pin commits)
sources/

# artifacts generated by CI or local builds
*.img
*.bin
*.itb
*.dtb
*.dtbo
*.tar
*.tar.gz
*.tar.zst
*.xz
*.zst

# allow hardware capture and vendor DTBs in version control
!captures/**/*.dtb
!vendor/**/*.dtb
!vendor/**/*.dts

# editor cruft
.vscode/
.DS_Store
