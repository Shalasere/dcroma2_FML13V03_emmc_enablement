# build outputs
/build/
/out/
/dist/
/tmp/

# fetched upstream trees (we don’t vendor these; we pin commits)
sources/

# artifacts generated by CI or local builds
*.img
*.bin
*.itb
*.dtb
*.dtbo
*.tar
*.tar.gz
*.tar.zst
*.xz
*.zst

# ignore noisy local artifacts (captures are intentionally local by default)
captures/
tmp/
*.log
dtbs_all/

# vendor DTB/DTS snapshots can be versioned
!vendor/**/*.dtb
!vendor/**/*.dts

# editor cruft
.vscode/
.DS_Store
