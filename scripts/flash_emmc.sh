#!/usr/bin/env bash
set -euo pipefail

cat <<'MSG'
flash_emmc.sh is a stub to prevent accidental data loss.

Fill this in with board-specific steps once the partition layout is agreed.
Expected inputs: image path, target device, and verification step.
MSG

exit 1
