#!/usr/bin/env bash
set -euo pipefail

cat <<'MSG'
flash_spi.sh is a stub to prevent accidental flashing.

Fill this in with board-specific steps once the SPI image format is known.
Expected inputs: SPI image path, target device, and verification step.
MSG

exit 1
